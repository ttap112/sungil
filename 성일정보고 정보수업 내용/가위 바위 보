1
a와 b가 가위바위보를 통해 누가 이겼는지 정하는 코드를 작성해보자
가위는 1 주먹은 2 보자기는 3
단 비기는 경우는 없음

a=int(input()) #수를 입력받음
b=int(input()) #수를 입력받음
if a>b:        #만약 a가>b보다 클 경우 a가 승리
  print("a승리")
else :         #만약 b가>a보다 클 경우 b가 승리
  print("b승리")

1-1
elif문을 활용하여 비기는경우, 보를 가위가 이기는 경우를 포함하야 코드를 짤것(노가다;;)
a=int(input()) #수를 입력받음
b=int(input()) #수를 입력받음
